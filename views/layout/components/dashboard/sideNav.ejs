<nav class="sb-sidenav accordion sb-sidenav-dark" id="sidenavAccordion">
    <div class="sb-sidenav-menu">
        <div class="nav">
            <div class="sb-sidenav-menu-heading">Home</div>
            <a class="nav-link" href="/">
                <div class="sb-nav-link-icon"><i class="fas fa-tachometer-alt"></i></div>
                Dashboard
            </a>
            <div class="sb-sidenav-menu-heading">Page</div>
            <% if (admin !== undefined && superadmin.length === 0) { %>
                <a class="nav-link" href="/account">
                    <div class="sb-nav-link-icon"><i class="fas fa-chart-area"></i></div>
                    User List
                </a>
            <% } else { %>
                <a class="nav-link" href="/account">
                    <div class="sb-nav-link-icon"><i class="fas fa-chart-area"></i></div>
                    Account List
                </a>
            <% } %>
            <a class="nav-link" href="/product">
                <div class="sb-nav-link-icon"><i class="fas fa-chart-area"></i></div>
                Product List
            </a>
            <a class="nav-link" href="/sales">
                <div class="sb-nav-link-icon"><i class="fas fa-table"></i></div>
                Selling List
            </a>
            <% if (superadmin !== undefined && admin.length === 0) { %>
            <div class="sb-sidenav-menu-heading">Superadmin</div>
            <a class="nav-link" href="/log">
                <div class="sb-nav-link-icon"><i class="fas fa-table"></i></div>
                Data Log
            </a>
            <% } %>
        </div>
    </div>
    <div class="sb-sidenav-footer">
        <div class="small">Logged in as</div>
        <!-- Username -->
        <div class="fs-5"><%= superadmin.username || admin.username %></div>
        <!-- Role -->
        <div class="small opacity-75"><%= superadmin.role || admin.role %></div>
    </div>
</nav>